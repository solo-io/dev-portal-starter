name: ZZ B dispatch inputs
on:
  push:
    branches: [Charlesthebird/nightlyCves]
  workflow_dispatch:
    inputs:
      model:
        description: "Model id, as the gateway names it. Blank uses the default."
        required: false
        type: string
      open_pr:
        description: "Open a PR (uncheck for a report-only dry run)"
        required: false
        type: boolean
        default: true
jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - run: echo ok
