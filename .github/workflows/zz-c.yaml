name: ZZ C permissions and concurrency
on:
  push:
    branches: [Charlesthebird/nightlyCves]
permissions:
  contents: write
  pull-requests: write
concurrency:
  group: nightly-ui-cve-fix
  cancel-in-progress: false
jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - run: echo ok
