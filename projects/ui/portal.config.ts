import { PortalConfig } from "./portal.config.types";

export const portalConfig: PortalConfig = {
  oidc: {
    clientId: "oidc_client_id",
    clientSecret: "oidc_client_secret",
  },
  isBoilerplate: true,
};
