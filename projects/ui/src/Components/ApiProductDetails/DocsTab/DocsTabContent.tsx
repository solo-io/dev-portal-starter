import { ApiVersion } from "../../../Apis/api-types";

const DocsTabContent = ({
  selectedApiVersion,
}: {
  selectedApiVersion: ApiVersion;
}) => {
  return <div>DocsTabContent</div>;
};

export default DocsTabContent;
